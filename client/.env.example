# Local development (uses Vite proxy to server on localhost:5000)
VITE_API_URL=/api

# Production example:
# Set this in Netlify environment variables to your deployed backend API root.
# If your backend base URL is https://api.example.com, set:
# VITE_API_URL=https://api.example.com/api
